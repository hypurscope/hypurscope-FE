import React from "react";

type VaultSummaryProps = {
  address: string;
};

const VaultSummary = ({ address }: VaultSummaryProps) => {
  return <div>VaultSummary</div>;
};

export default VaultSummary;

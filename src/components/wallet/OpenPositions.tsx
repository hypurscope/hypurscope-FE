import React from 'react'

type OpenPositionsProps = {
  address: string
}

const OpenPositions = ({address}: OpenPositionsProps) => {
  return (
    <div>OpenPositions</div>
  )
}

export default OpenPositions